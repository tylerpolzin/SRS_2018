<%= form_with(model: upload_batch, local: true) do |form| %>
  <% if upload_batch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(upload_batch.errors.count, "error") %> prohibited this upload_batch from being saved:</h2>

      <ul>
      <% upload_batch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
